services:
  chatbot:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data 
    environment:
      - USE_LOCAL=true
      - ENABLE_RAG=true
      - GROQ_API_KEY=${GROQ_API_KEY}
      - OLLAMA_API_KEY=${OLLAMA_API_KEY}
